apiVersion: v1
kind: Service
metadata:
  name: civicflow-frontend
spec:
  selector:
    app: civicflow-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
